/* Second Line Crew Call — connection settings.
   Fill these two in after you create the Supabase project (Project Settings →
   API). Both are safe to publish: the anon key only permits what the database
   rules in _setup/01-schema.sql allow. */
window.SLPC_CONFIG = {
  SUPABASE_URL: "PASTE_YOUR_PROJECT_URL_HERE",
  SUPABASE_ANON_KEY: "PASTE_YOUR_ANON_PUBLIC_KEY_HERE",
  POSTER_BUCKET: "posters"
};
